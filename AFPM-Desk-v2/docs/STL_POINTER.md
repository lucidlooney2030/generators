# STL / OpenSCAD package note — AFPM-Desk-v2

Large binary STL files often cannot upload intact through the Google Drive connector.

**Local package on Magneat-o computer:**
- Docs + STLs: `/workspace/generator-experiments/v2/`
- Archive: `/workspace/afpm_desk_v2_stls.tar.gz` (~1.8 MB of stl+scad+scripts)

**Please drop these into this Drive folder (`Generator Experiments / v2`) from the chat archive or Magneat-o desktop:**
- All files under `stl/*.stl` (17 binary STLs)
- Optionally `scad/*.scad` and `scripts/export_stls.sh`

Docs in this folder (README, BUILD, PARTS, SCHEMATIC, PHYSICS, PRINT_NOTES, FIT_COUPON, MANIFEST, DEBRIEF) are already uploaded as text.
